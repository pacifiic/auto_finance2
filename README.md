# 🚀 Crypto Trading Automation Library

코인 매매 자동화를 위한 기술적 분석 라이브러리입니다.

## 📌 주요 기능

- **15+ 기술적 지표**: 추세, 모멘텀, 변동성, 거래량 지표
- **20+ 캔들스틱 패턴**: 해머, 잉걸핑, 모닝스타 등
- **차트 패턴 인식**: 더블탑/바텀, 헤드앤숄더, 삼각형 등
- **지지/저항 및 피보나치**: 자동 레벨 감지
- **가중치 기반 신호 조합**: 다양한 조합 방법 지원
- **프리셋 시스템**: aggressive, conservative, swing, scalping 등
- **하이퍼파라미터 관리**: 중앙화된 설정 관리

## 🏗 프로젝트 구조

```
auto_finance2/
├── config/
│   └── patterns.yaml          # 패턴 및 하이퍼파라미터 정의
├── crypto_trading/
│   ├── indicators/            # 기술적 지표
│   │   ├── trend.py          # SMA, EMA, MACD, ADX
│   │   ├── momentum.py       # RSI, Stochastic, Williams %R, CCI
│   │   ├── volatility.py     # Bollinger, ATR, Keltner
│   │   └── volume.py         # OBV, MFI, VWAP
│   ├── patterns/              # 패턴 인식
│   │   ├── candlestick.py    # 캔들스틱 패턴
│   │   └── chart_patterns.py # 차트 패턴
│   ├── signals/               # 신호 생성
│   │   ├── generator.py      # 신호 생성 엔진
│   │   └── combiner.py       # 신호 조합기
│   ├── strategy/              # 전략
│   │   ├── engine.py         # 전략 엔진
│   │   └── hyperparameters.py # HP 관리
│   └── utils/                 # 유틸리티
├── examples/                   # 사용 예시
│   ├── demo.py               # 전체 기능 데모
│   └── quick_start.py        # 빠른 시작
└── requirements.txt
```

## ⚡ 빠른 시작

```python
from crypto_trading.strategy import StrategyEngine
from crypto_trading.utils import generate_sample_data

# 1. 데이터 로드 (샘플 또는 실제 데이터)
df = generate_sample_data(n_samples=300)

# 2. 전략 엔진 초기화
engine = StrategyEngine()

# 3. 분석 수행
analysis = engine.analyze(df)

# 4. 결과 확인
print(f"신호: {analysis['signal']['type']} ({analysis['signal']['value']:.3f})")
print(f"추천: {analysis['recommendation']['action']}")
```

## 📊 지원 지표 목록

### 추세 지표 (Trend)
| 지표 | 설명 | 신호 |
|------|------|------|
| SMA | 단순 이동평균 | Golden/Death Cross |
| EMA | 지수 이동평균 | 추세 정렬 |
| MACD | 이동평균 수렴확산 | 크로스오버, 다이버전스 |
| ADX | 평균 방향 지수 | 추세 강도 |

### 모멘텀 지표 (Momentum)
| 지표 | 설명 | 신호 |
|------|------|------|
| RSI | 상대강도지수 | 과매수/과매도, 다이버전스 |
| Stochastic | 스토캐스틱 | K/D 크로스오버 |
| Williams %R | 윌리엄스 %R | 과매수/과매도 |
| CCI | 상품채널지수 | 0 교차, 극단값 |

### 변동성 지표 (Volatility)
| 지표 | 설명 | 신호 |
|------|------|------|
| Bollinger | 볼린저 밴드 | 밴드 터치, 스퀴즈 |
| ATR | 평균 진폭 | 변동성 수준 |
| Keltner | 켈트너 채널 | 채널 돌파 |

### 거래량 지표 (Volume)
| 지표 | 설명 | 신호 |
|------|------|------|
| OBV | 거래량 균형 | 다이버전스, 추세 확인 |
| MFI | 자금 흐름 지수 | 과매수/과매도 |
| VWAP | 거래량 가중 평균가 | 가격 위치 |

## 📈 캔들스틱 패턴

### 상승 반전 패턴
- Hammer (해머)
- Inverted Hammer (역해머)
- Bullish Engulfing (상승 장악형)
- Morning Star (샛별형)
- Piercing Line (관통형)
- Three White Soldiers (적삼병)

### 하락 반전 패턴
- Hanging Man (교수형)
- Shooting Star (유성형)
- Bearish Engulfing (하락 장악형)
- Evening Star (저녁별형)
- Dark Cloud Cover (먹구름형)
- Three Black Crows (흑삼병)

## ⚙️ 하이퍼파라미터 조절

### 프리셋 사용
```python
engine = StrategyEngine()

# 공격적 전략 (단기, 빈번한 거래)
engine.apply_preset('aggressive')

# 보수적 전략 (장기, 확실한 신호만)
engine.apply_preset('conservative')

# 스윙 트레이딩
engine.apply_preset('swing')

# 스캘핑
engine.apply_preset('scalping')
```

### 개별 파라미터 조절
```python
# RSI 설정 변경
engine.update_hyperparameters({
    'momentum.rsi.period': 7,
    'momentum.rsi.overbought': 75,
    'momentum.rsi.oversold': 25
})

# 가중치 조절
engine.set_weights({
    'category.trend': 2.0,      # 추세 지표 강조
    'category.momentum': 1.5,   # 모멘텀 지표 강조
    'category.volatility': 0.5  # 변동성 지표 약화
})
```

## 🔄 신호 조합 방법

```python
from crypto_trading.signals import SignalCombiner

# 가중 평균 (기본값)
combiner = SignalCombiner({'method': 'weighted_average'})

# 투표 방식
combiner = SignalCombiner({'method': 'voting'})

# 합의 방식 (대부분 일치 시에만 신호)
combiner = SignalCombiner({'method': 'consensus'})

# 최대/최소값
combiner = SignalCombiner({'method': 'maximum'})
combiner = SignalCombiner({'method': 'minimum'})
```

## 📉 신호 해석

| 신호 값 | 분류 | 의미 |
|---------|------|------|
| ≥ 0.7 | strong_buy | 강력 매수 신호 |
| 0.3 ~ 0.7 | buy | 매수 신호 |
| 0.1 ~ 0.3 | weak_buy | 약한 매수 신호 |
| -0.1 ~ 0.1 | neutral | 중립 |
| -0.3 ~ -0.1 | weak_sell | 약한 매도 신호 |
| -0.7 ~ -0.3 | sell | 매도 신호 |
| ≤ -0.7 | strong_sell | 강력 매도 신호 |

## 🛡 리스크 관리

분석 결과에는 자동으로 리스크 파라미터가 포함됩니다:

```python
analysis = engine.analyze(df)

risk = analysis['risk_parameters']
print(f"손절가: ${risk['stop_loss']:,.2f}")
print(f"익절가: ${risk['take_profit']:,.2f}")
print(f"권장 포지션 크기: {risk['position_size']:.1%}")
```

## 🔮 향후 계획 (TODO)

### 🟢 완료됨
- [x] 백테스팅 시스템 (`scripts/backtester.py`)
- [x] Optuna 하이퍼파라미터 최적화 (`scripts/optuna_tuner.py`)
- [x] RL Pair Trading 기본 구현 (`rl_trading/`)
- [x] A2C 에이전트 (논문 기반) (`rl_trading/a2c_agent.py`)

### 🟡 진행 중
- [ ] 확장된 데이터 수집 (20개 코인, 2020-2026년 6년 데이터)

### 🔴 예정 (TODO)

#### 1. Online Learning 전환
- [ ] 테스트셋 없이 실시간 학습하는 Online RL 구현
- [ ] 시계열 순서대로 모킹 데이터 생성 및 학습
- [ ] Rolling window 기반 지속적 학습

#### 2. 경제 상황 반영 시스템
- [ ] 경제 상황 4단계 분류 (저금리/고금리/확장기/침체기)
- [ ] 내재 변동성 (Implied Volatility) 데이터 수집 및 반영
- [ ] 시간에 따른 금리 데이터 수집 (Fed Funds Rate, 한국 기준금리)
- [ ] SNS 데이터 수집 및 모니터링:
  - [ ] Elon Musk 트위터/X 포스팅 수집
  - [ ] Trump 소셜미디어 포스팅 수집
  - [ ] 주요 인플루언서 발언 추적
- [ ] 뉴스 데이터 수집 및 모니터링:
  - [ ] Reuters, Bloomberg, CoinDesk 등 주요 언론사
  - [ ] 실시간 뉴스 스크래핑
  - [ ] 뉴스 감성 분석 (Sentiment Analysis)
  - [ ] 데이터셋 자동 구축 파이프라인

#### 3. 실거래 연동
- [ ] 실시간 거래소 API 연동 (Binance, Upbit 등)
- [ ] 자동 매매 실행
- [ ] 리스크 관리 시스템

#### 4. 추가 개선
- [ ] ML 기반 신호 최적화
- [ ] LightGBM/XGBoost 분류 모델
- [ ] 웹 대시보드

## 📜 라이선스

MIT License

## ⚠️ 주의사항

이 라이브러리는 교육 및 연구 목적으로 제작되었습니다. 
실제 투자에 사용 시 발생하는 손실에 대해 책임지지 않습니다.
암호화폐 투자는 높은 위험을 수반하며, 투자 결정은 본인의 판단과 책임 하에 이루어져야 합니다.
